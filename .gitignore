# macOS
.DS_Store
**/.DS_Store

# Node (the neural/ prototype has its own .gitignore covering
# node_modules and dist; this is a belt-and-braces guard)
node_modules/
**/node_modules/

# Local editor / tooling state
.vscode/
.idea/
.claude/

# Promo video renders and their recording scripts. These are working files,
# not part of the published site — committing them ships ~24 MB of MP4 to
# the live deploy for no reason.
ad/
